/* =========================================================================
   Diffraction Lab — lattice data
   Phase 1: stub. Phase 4 will define lattice parameters and structure
   factors for each crystal system; or load from /data/crystal-systems.json.
   ========================================================================= */

(function () {
  'use strict';

  window.TEM = window.TEM || {};
  window.TEM.latticeData = {
    // systems: { cubic-simple: {...}, cubic-bcc: {...}, ... } — Phase 4
  };
})();
