# Module 2 v6 changes

- DEF/STIG starts with no selected mode, so the learner must click SHIFT during beam centring. The SHIFT button then receives the standard selected-button highlight.
- The camera drawer automatically scrolls to each pending control in the Insert Camera → Live View → Raise Fluorescent Screen sequence.
- Restart now performs a full reload of `pages/microscope-guided.html`, clearing all transient state and UI effects.
