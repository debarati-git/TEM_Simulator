# Module 2 viewport reserved-area update

A permanent 28 px safe area is reserved below both the Column and Viewing Screen panels. The reserved space matches the collapsed PC drawer strip height, so the bottom of the column image and phosphor screen remain fully visible.

The Column hotspot layer remains inside the resized panel and continues to map positions against the actual rendered image bounds.
