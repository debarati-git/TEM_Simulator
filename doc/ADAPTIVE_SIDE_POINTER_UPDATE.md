# Adaptive floating pointer update

- Uses a sideways emoji hand when sufficient horizontal room exists.
- Left-side cue uses 👉 and right-side cue uses 👈 so the finger points toward the active control.
- Falls back to the existing downward 👇 cue where horizontal space is insufficient.
- All directions retain the same size, drop shadow, and animated cue style.
